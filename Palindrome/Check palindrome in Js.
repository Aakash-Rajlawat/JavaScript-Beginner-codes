let isPalindrome = (x)=>{
    let compare;
    compare = x;
    let reverse=0;
    let remainder; 
    while (x>0){
    remainder = x%10;
    reverse =reverse*10+ remainder;
    x = Math.floor(x/10);
    }
     return (compare == reverse) ? true : false;
}
isPalindrome(545)
